/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.githubusercontent.com",
      "i.scdn.co",
      "openweathermap.org",
    ],
  },
};

module.exports = nextConfig;
